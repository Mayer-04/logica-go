package main

/*
* Sync.Mutex
Sync.Mutex es una estructura en Go que proporciona primitivas de sincronización para
controlar el acceso a recursos compartidos entre varias goroutines.

- Esto garantiza que podamos controlar qué goroutinas pueden acceder a ciertos datos en qué momento.

- Lock (Bloquear): Bloquea el acceso a la sección de código protegida.
- Unlock (Desbloquear): Libera el bloqueo para permitir que otras goroutines accedan.

Para evitar olvidarnos de liberar el bloqueo, es recomendable usar `defer`.

*/

func main() {

}
